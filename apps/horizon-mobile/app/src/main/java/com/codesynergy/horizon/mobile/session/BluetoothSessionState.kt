package com.codesynergy.horizon.mobile.session

enum class BluetoothSessionState {
    DISCONNECTED,
    CONNECTING,
    INITIALIZING,
    CONNECTED,
    RECONNECTING,
    READING,
    STOPPING,
    ERROR,
}
